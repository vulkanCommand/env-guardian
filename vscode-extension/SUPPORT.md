# Support

- Email: `gdkalyan2109@gmail.com`
- Issues: `https://github.com/vulkanCommand/env-guardian/issues`

The extension shells out to the `envguard` CLI. If a command fails, first confirm the CLI works in the same workspace:

```bash
envguard validate
envguard security
```
